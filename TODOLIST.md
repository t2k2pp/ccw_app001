# AI開発者向け ToDoリスト (Local AI Slide Creator)

このToDoリストは、提供された設計ドキュメント群に基づき、AI開発者が実装を進めるためのタスクを順序立てて記述したものです。各タスクは、依存関係とビルディングブロックのアプローチを考慮して配置されています。

**TDD (テスト駆動開発) の遵守**: `CLAUDE.md` の規約に従い、すべての機能実装において、まず失敗するテストを書き、それをパスさせる形で開発を進めてください。

---

## Phase 0: プロジェクト基盤の構築

このフェーズでは、開発を開始するための環境と、アプリケーションの骨格となる基本的な構造を構築します。

### 1. 開発環境のセットアップ
- [ ] **リポジトリ初期化**: Gitリポジトリをセットアップします。
- [ ] **Lint/Formatter設定**: `CLAUDE.md` に基づき、ESLint, Prettier, stylelintを設定します。
- [ ] **Git Hooks設定**: Huskyとlint-stagedを導入し、コミット時に自動でコードフォーマットとリンターが実行されるようにします。
- [ ] **Viteプロジェクト作成**: Viteを使い、React + TypeScriptのフロントエンドプロジェクトを初期化します。
- [ ] **Expressプロジェクト作成**: `server` ディレクトリを作成し、Node.js + Express + TypeScriptのバックエンドプロジェクトを初期化します。
- [ ] **Docker環境構築**: `ARCHITECTURE.md` に記載の `docker-compose.yml` を作成し、フロントエンドとバックエンドのコンテナを定義します。

### 2. コアライブラリの導入と設定
- [ ] **状態管理**: `zustand` を導入し、状態管理の基本構造を準備します。
- [ ] **UIコンポーネント**: `tailwindcss` を導入し、設定ファイル (`tailwind.config.js`) を作成します。
- [ ] **キャンバス**: `konva` と `react-konva` を導入します。
- [ ] **IndexedDB**: `idb` を導入します。
- [ ] **バリデーション**: `zod` をフロントエンドとバックエンドの両方に導入します。

### 3. アプリケーションの骨格作成
- [ ] **型定義**: `DATA_MODEL.md` と `ARCHITECTURE.md` に基づき、`src/types` ディレクトリに主要な型（`Project`, `Slide`, `SlideElement`, `Asset` 等）を定義します。
- [ ] **ディレクトリ構造**: `ARCHITECTURE.md` に示されているフロントエンドのディレクトリ構造（`components`, `services`, `stores`, `repositories`, `hooks` 等）を作成します。
- [ ] **ルーティング設定**: `react-router-dom` を導入し、主要なページ（ホーム画面、エディタ画面）のルーティングを設定します。
- [ ] **基本UIレイアウト**: `FEATURES.md` のUI/UX設計に基づき、ヘッダー、サイドバー、メインコンテンツエリアから成るアプリケーションの基本レイアウトコンポーネントを作成します。

---

## Phase 1: MVP (Minimum Viable Product) の実装

このフェーズでは、ユーザーがスライドを生成し、基本的な編集を行い、保存・エクスポートできるという中核的な価値を提供することを目指します。

### 1. バックエンド (最小限) の実装
- [ ] **サーバー起動**: Expressサーバーの基本的な起動スクリプト (`server/index.ts`) を実装します。
- [ ] **データベース設定**: `better-sqlite3` を導入し、`server/db/schema.sql` に基づいてシステムデータベース (`system.db`) を初期化するスクリプトを作成します。
- [ ] **データシード**: プリセットテンプレート、テーマ、アイコンの初期データをSQLiteデータベースに投入するシードスクリプトを作成します。
- [ ] **システムデータAPI**: `API.md` に基づき、以下の読み取り専用APIエンドポイントを実装します。
    - [ ] `GET /api/v1/templates`
    - [ ] `GET /api/v1/templates/:id`
    - [ ] `GET /api/v1/icons/search`
    - [ ] `GET /api/v1/themes`
- [ ] **AIプロキシAPI**: `API.md` に基づき、LLMコンテンツ生成用のプロキシエンドポイント (`POST /api/v1/ai/generate-structure`, `POST /api/v1/ai/generate-content`) を実装します。

### 2. フロントエンド: データ永続化層
- [ ] **IndexedDBサービス**: `STORAGE.md` の `IndexedDBService` クラスを実装し、データベースの初期化と基本的なCRUD操作を可能にします。
- [ ] **LocalStorageサービス**: `STORAGE.md` の `LocalStorageService` クラスを実装し、設定情報の読み書きを可能にします。
- [ ] **リポジトリ層**: `ARCHITECTURE.md` に基づき、`ProjectRepository`, `SlideRepository`, `AssetRepository` を実装し、IndexedDBサービスを介したデータの抽象化を行います。
- [ ] **Zustandストア**: プロジェクト状態を管理する `projectStore` と、スライド一覧を管理する `slideStore` を作成します。

### 3. フロントエンド: プロジェクト管理機能
- [ ] **ホーム画面UI**: 最近使ったプロジェクト一覧と、新規作成ボタン（空白から、テンプレートから、AIから）を配置したホーム画面を実装します。
- [ ] **プロジェクト作成**: 新規プロジェクトを作成し、IndexedDBに保存する機能を実装します。
- [ ] **プロジェクト読み込み**: プロジェクトIDを元にIndexedDBからデータを読み込み、Zustandストアを初期化する機能を実装します。

### 4. フロントエンド: コアエディタ機能
- [ ] **キャンバスコンポーネント**: `Konva.js` を用いて、スライドを表示するメインキャンバス (`Canvas.tsx`) を作成します。
- [ ] **要素のレンダリング**: `SlideElement` の型に応じて、テキスト、画像、図形をキャンバス上にレンダリングするコンポーネントを作成します。
- [ ] **要素の選択と操作**: 要素のクリックによる選択、ドラッグによる移動、リサイズハンドルによるサイズ変更の基本機能を実装します。
- [ ] **スライドリスト**: 画面左側にスライドのサムネイル一覧を表示し、スライドの選択とドラッグ&ドロップによる並び替え機能を実装します。
- [ ] **プロパティパネル**: 画面右側に、選択された要素のプロパティ（位置、サイズ、色など）を表示・編集するためのUIを実装します。

### 5. フロントエンド: AI生成機能 (MVP)
- [ ] **AIクライアントサービス**: `AIClient` サービス (`services/ai/aiClient.ts`) を実装し、バックエンドのAIプロキシAPIを呼び出すためのロジックを記述します。
- [ ] **AIによる構成生成**: トピックを入力するとスライド構成（タイトルと箇条書き）を生成し、それを元に複数のスライドを一度に作成する機能を実装します。
- [ ] **AIによるコンテンツ生成**: 選択したテキストボックスに対して、AIによるコンテンツ生成や改善提案を行う機能を実装します。

### 6. フロントエンド: エクスポート/インポート機能 (MVP)
- [ ] **JSONエクスポート**: `STORAGE.md` の仕様に基づき、現在のプロジェクトを画像データ（Base64）を含んだ単一のJSONファイルとしてエクスポートする機能を実装します。
- [ ] **JSONインポート**: エクスポートされたJSONファイルを読み込み、プロジェクトを復元する機能を実装します。
- [ ] **PDFエクスポート**: `jsPDF` を利用し、クライアントサイドで現在のスライドをPDFとしてエクスポートする機能を実装します。`STORAGE.md` の `PDFExporter` を参考にします。

---

## Phase 2: AI強化と高度な編集機能

MVPの基盤の上に、より豊かな表現力と効率化のための機能を追加します。

- [ ] **AI画像生成**: Stable DiffusionプロキシAPIと連携し、プロンプトから画像を生成してスライドに挿入する機能を実装します。
- [ ] **アセット管理**: 画像のアップロード機能と、プロジェクトで使用するアセットを一覧表示・管理するUIを実装します。
- [ ] **履歴管理 (Undo/Redo)**: `historyStore` を実装し、ユーザーの操作履歴を記録・再生することでUndo/Redo機能を実現します。
- [ ] **ZIPエクスポート/インポート**: `JSZip` を利用し、プロジェクトデータとアセットファイルをZIP形式でまとめてエクスポート/インポートする機能を実装します。
- [ ] **Marpエクスポート/インポート**: `STORAGE.md` の仕様に基づき、プロジェクトをMarp（Markdown）形式にエクスポート、またMarpファイルからインポートする機能を実装します。
- [ ] **アイコンライブラリ**: バックエンドのアイコンAPIと連携し、アイコンを検索してスライドに挿入する機能を実装します。
- [ ] **SVGサポート**: SVGファイルをアセットとしてインポートし、キャンバス上に表示する機能を実装します。

---

## Phase 3: プレゼンテーションとインタラクティブ機能

作成したスライドを発表・活用するための機能を追加します。

- [ ] **プレゼンテーションモード**: スライドをフルスクリーンで表示し、ノートやタイマーを備えた発表者ビューを実装します。
- [ ] **デジタルサイネージモード**: スライドの自動再生、ループ、トランジション効果を設定できるモードを実装します。
- [ ] **音声合成 (TTS)**: `Web Speech API` を利用し、スライドのノートや専用スクリプトを音声で読み上げる機能を実装します。
- [ ] **PPTXエクスポート**: `pptxgenjs` を利用し、プロジェクトをPowerPoint形式でエクスポートする機能を実装します。
- [ ] **HTMLエクスポート**: `reveal.js` のテンプレートを使い、プロジェクトをスタンドアロンのHTMLプレゼンテーションとしてエクスポートする機能を実装します。
- [ ] **WebSocket連携**: バックエンドにWebSocketを導入し、長時間のAI処理やエクスポート処理の進捗をリアルタイムにUIに通知する仕組みを構築します。

---

## Phase 4 & 5: エコシステムと将来の拡張

アプリケーションをプラットフォームとして成長させるための機能を追加します。

- [ ] **カスタムテンプレート作成**: ユーザーが独自のスライドレイアウトをテンプレートとして保存できる機能を実装します。
- [ ] **カスタムテーマ作成**: ユーザーが独自の配色やフォントをテーマとして保存できる機能を実装します。
- [ ] **動画エクスポート**: スライドショーと合成音声を元に、MP4形式の動画ファイルを生成するサーバーサイドエクスポート機能を実装します。
- [ ] **ローカルTTSエンジン統合**: Coqui TTSやPiperなど、ローカルで動作するTTSエンジンとの連携機能を実装します。
- [ ] **プラグイン機構の設計**: サードパーティ開発者が機能を追加できるプラグインシステムの基礎を設計・実装します。
- [ ] **リアルタイムコラボレーションの検討**: `Review.md` の提案に基づき、CRDTsなどの技術を調査し、将来のコラボレーション機能に向けたプロトタイプを開発します。
